# 🚀 Pixlog AI 智能洞察模块 PRD (v1.0)

> 整理日期：2026年1月24日
> 状态：需求讨论阶段

---

## 1. 模块概述

本模块旨在利用用户在 Mac 上的高频、多维度行为数据，通过 AI（如 GPT-4o, Claude 等）进行深度解析，为用户提供具有**情绪价值、逻辑深度和可执行建议**的效能总结。

**核心价值主张：** Pixlog 不仅仅记录时间，它还**懂**时间。

---

## 2. 核心功能点

### 2.1 智能洞察建议 (AI Insights)

| 功能 | 描述 |
|------|------|
| **三种语气切换** | 用户可自由选择"硬核教练（毒舌鞭策）"、"贴心助手（温柔鼓励）"或"理性专家（数据审计）"三种风格的报告 |
| **长文深度报告** | 报告不再是简短的评价，而是一篇包含背景、行为诊断、趋势预测和战术指令的长文 |
| **单日简报 & 周度复盘** | 支持按日和按周自动生成报告，用户可随时查阅 |

### 2.2 行为特征深挖 (Behavioral Fingerprints)

| 功能 | 描述 |
|------|------|
| **心流干扰源追踪** | 自动识别哪些 App（如微信、网页）频繁中断了主力应用（如 Figma, Xcode）的使用，揪出"心流杀手" |
| **心流步长分析** | 统计用户的连续专注时长，并锁定用户的"黄金状态"发生时段 |
| **作息能量模型** | 分析开始/结束工作时间，识别深夜/通宵等异常作息 |

### 2.3 趋势对比与地位评估

| 功能 | 描述 |
|------|------|
| **纵向趋势对比** | 自动计算本周对比上周（或本月对比上月）的效率波动（时长、切换率、目标达成率） |
| **历史成就系统** | 自动捕捉"历史个人之最"（如：有史以来最高专注日），并给出类似"击败全球 90% 开发者"的社交评价 |

### 2.4 AI 反向建议 (Smart Recognition)

| 功能 | 描述 |
|------|------|
| **潜在主力识别** | 当 AI 发现用户在高频使用某个未标记的生产力 App 或域名（如 `linear.app`）时，主动询问并建议将其设为主力应用 |

---

## 3. 技术实现逻辑：AI 预处理 11 点

为了保证 AI 分析的准确性与隐私安全，Pixlog 会在本地将 `sessions.json` 原始数据预处理为以下 **11 个维度** 的摘要报表发送给 AI：

| 序号 | 维度 | 说明 |
|------|------|------|
| 1 | **全量主力数据** | 所有勾选为主力的 App 和域名的详细使用时长 |
| 2 | **非主力消耗大户** | 时长排名前五的非主力 App（揭示摸鱼黑洞） |
| 3 | **浏览器深挖** | 前五大高频访问域名（区分网页工作与摸鱼） |
| 4 | **打断次数统计** | 总切换 Session 数量（反映大脑碎片化程度） |
| 5 | **干扰源排名** | 统计谁最常在主力工作期间"横插一脚" |
| 6 | **目标达成率** | 对比设置的每日目标时长（如 4h）与实际达成情况 |
| 7 | **作息轨迹** | 提取每日最早/最晚记录，分析工作节奏 |
| 8 | **专注峰值** | 本周最长一段不切换应用的使用时长 |
| 9 | **碎片化指数** | 统计短于 1 分钟的 Session 占比 |
| 10 | **趋势差值** | 关键指标较上个周期的百分比变化 |
| 11 | **社交排名数据** | 在全体用户或历史记录中的百分比排行 |

---

## 4. 数据结构定义 (Swift)

```swift
struct AIDataPayload: Codable {
    // 基础信息
    let weekRange: String                  // 如 "1/18 - 1/24"
    let totalActiveTime: String            // 总活跃时长
    let focusRate: Double                  // 主力时长 / 总时长
    
    // 应用分析
    let primarySummary: [AppDetail]        // 所有勾选为主力的应用/域名情况
    let distractionSummary: [AppDetail]    // 时长较长的非主力应用
    let browserDomains: [DomainDetail]     // 浏览器内的域名细分
    
    // 行为指纹
    let totalInterruptions: Int            // 总切换次数
    let primaryDistractors: [String: Int]  // 应用名 : 打断主力应用的次数
    let fragmentationIndex: Double         // 碎片化指数 (0-1)
    
    // 目标与作息
    let dailyGoalHours: Double             // 设定的目标（如 4.0）
    let daysGoalReached: Int               // 本周达标天数
    let averageStartTime: String           // 平均开始工作时间
    let averageEndTime: String             // 平均结束时间
    let hasOvernightSession: Bool          // 是否有通宵/深夜记录
    
    // 专注表现
    let longestFocusDuration: String       // 本周最长连续专注时长
    let longestFocusApp: String            // 发生在哪个 App 上
    
    // 趋势对比
    let weekOverWeekChange: TrendChange    // 本周 vs 上周
    
    // AI 智能识别
    let unknownHighUsageApps: [String]     // 高时长但未分类的应用
    
    // 社交排名
    let historicalRank: String             // 如 "有史以来最高专注日"
    let globalPercentile: Int?             // 如 "击败 90% 用户"（可选，需要后端支持）
    
    struct AppDetail: Codable {
        let name: String
        let duration: String
        let isPrimary: Bool
        let switchCount: Int
    }
    
    struct DomainDetail: Codable {
        let domain: String
        let duration: String
        let isPrimary: Bool
    }
    
    struct TrendChange: Codable {
        let focusTimeChange: Double        // 百分比变化
        let interruptionChange: Double
        let goalAchievementChange: Double
    }
}
```

---

## 5. UI 交互建议

| 建议 | 描述 |
|------|------|
| **卡片式布局** | AI 洞察以精美的卡片形式展示，背景色随当前"专注状态"变化（绿/红） |
| **左右滑动切屏** | 在周报页面，用户可以通过左右滑动，在"教练、助手、专家"三个分身之间快速切换 |
| **一键转发图** | 提供长图分享功能，将"历史成就"和"AI 金句"生成适合朋友圈或 X 的视觉图 |
| **智能开关** | 在设置中增加"AI 智能识别建议"开关，保护用户不被过度打扰 |

---

## 6. Prompt 模板文件

已存储在同目录下：
- `Prompt_Hardcore_Coach.md` (硬核教练)
- `Prompt_Gentle_Assistant.md` (贴心助手)
- `Prompt_Rational_Expert.md` (理性专家)

---

## 7. 隐私与安全

| 措施 | 说明 |
|------|------|
| **本地预处理** | 原始 Session 数据不上传，仅发送统计摘要 |
| **数据脱敏** | 不发送具体时间戳，仅发送时长和排名 |
| **用户可控** | 提供"AI 洞察开关"，用户可随时关闭该功能 |
| **透明提示** | 首次使用时弹窗说明数据如何被使用 |

---

## 8. 后续待讨论

- [ ] AI API 供应商选择（OpenAI / Claude / 本地模型）
- [ ] 是否需要后端服务支持"全球排名"功能
- [ ] 付费策略：AI 洞察是否作为 Pro 功能

---

> 文档维护：shrock & AI Assistant
